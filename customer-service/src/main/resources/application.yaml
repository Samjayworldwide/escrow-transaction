server:
  port: 8085

spring:
  application:
    name: customer-service
  config:
    import: optional:/Users/mac/escrow_project/customer-service/.env[.properties]
  threads:
    virtual:
      enabled: true
  grpc:
    server:
      port: 9090
      reflection:
        enabled: true
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  cloud:
    function:
      definition: createCustomer;createCustomerDlq
    stream:
      bindings:
        createCustomer-in-0:
          destination: user-registered
          group: ${spring.application.name}
          consumer:
            max-attempts: 3
            back-off-initial-interval: 1000
            back-off-multiplier: 2.0
            back-off-max-interval: 10000
        createCustomerDlq-in-0:
          destination: create-customer.DLQ
          group: ${spring.application.name}-creation-dlq
      kafka:
        binder:
          brokers:
            - localhost:9092
        bindings:
          createCustomer-in-0:
            consumer:
              enable-dlq: true
              dlq-name: create-customer.DLQ

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
      show-components: always
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}

logging:
  level:
    root: INFO
    com.samjay: TRACE

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/